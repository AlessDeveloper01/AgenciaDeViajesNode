extends ./layout/index

block contenido

    h1.mt-5.text-center= pagina
    main.container.mt-5 
        include ./layout/bloque_viajes

            //- p= JSON.stringify(viajes) Forma de saber si se pasaron de manera correcta los datos